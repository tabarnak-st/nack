language: cpp
sudo: false
dist: trusty
script:
matrix:
  include:
    # works on debian-jessir
    - os: linux
      addons:
        apt:
          sources:
            - george-edison55-precise-backports
          packages:
            - cmake-data
            - cmake
          script:make
            

before_install:
         - ./test.sh 
